import React from 'react'

export default function Installments({parcela}) {
	//const {parcela} = props;
	/*
	let html = "<ul>";
	for (let i = 0; i < tempo; i++)
		html += `<li> ${juros} </li>`
	html += "</ul>"
	*/
	console.log(parcela)
	return (
		<div>

		</div>
	)
}
